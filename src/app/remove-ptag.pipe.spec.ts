import { RemovePTagPipe } from './remove-ptag.pipe';

describe('RemovePTagPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovePTagPipe();
    expect(pipe).toBeTruthy();
  });
});
